# Context — [Project Name]

A shared language for this project. Terms defined once, used everywhere — in
conversation, in code (variable/function/file names), and in the docs. A
glossary and **nothing else**: no plans, no implementation details, no decisions
(those live in the spec and DEVLOG).

Keep it short — a glossary you can read in 30 seconds is one the agent will
actually use. `/grill` maintains it inline during design sessions; seed it
lazily, only when a term is actually worth pinning down.

## Language

**[Term]**:
[What it is, precisely — and what it is *not* (the nearest concept it gets
confused with).]
_Avoid_: [looser synonyms that should not be used for this concept]

## Relationships

- A **[Term]** holds many **[Term]**
- [structural facts the definitions alone don't capture]

## Flagged ambiguities

- [words used two ways, and how they were resolved — or open ones, so the next
  session knows they're still live]
